import express from 'express';
import config from 'config';
import UploadImageFileRouter from './upload.router';
import QueryImageFileRouter from './queryfile.router';

export {
    UploadImageFileRouter,
    QueryImageFileRouter
}